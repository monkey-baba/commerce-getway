---
logging:
  file: app.log
  level:
    com.alibaba.nacos.client.naming: WARN
spring:
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:nacos}:${NACOS_PORT:8848}
        file-extension: yaml
  application:
    name: zuul



